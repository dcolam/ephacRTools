% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imagingTools.R
\name{plotThumbnails}
\alias{plotThumbnails}
\title{Display thumbnail images for selected wells from a SummarizedExperiment}
\usage{
plotThumbnails(
  se,
  wells,
  img_class,
  show = c("both", "BF", "fluoro"),
  col_name = "thumbnails",
  sep = ".",
  ncol = NULL,
  nrow = NULL,
  coldata_vars = NULL,
  label_size = 8,
  bg = "#1a1a1a"
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} with a nested thumbnail
\code{DataFrame} in \code{colData} (added by \code{addThumbnailPaths}).}

\item{wells}{Character vector of well identifiers in
\code{"<Well><sep><Plate_ID>"} format,
e.g. \code{c("B10.18T39383", "C05.18T39383")}.}

\item{img_class}{Class label to display, e.g. \code{"class1"}.}

\item{show}{Which images to show: \code{"both"} (BF + fluoro side by side,
default), \code{"BF"}, or \code{"fluoro"}.}

\item{col_name}{Name of the colData column holding the thumbnail paths.
Default: \code{"thumbnails"}.}

\item{sep}{Separator between Well and Plate_ID in \code{wells}.
Default: \code{"."}.}

\item{ncol}{Number of columns in the well grid. \code{NULL} (default) puts
all wells in a single row.}

\item{nrow}{Number of rows in the well grid. \code{NULL} (default) is
derived from \code{ncol}.}

\item{coldata_vars}{Optional character vector of \code{colData} columns
(e.g. \code{c("Condition", "QC")}) whose values are appended to each
well's title label.}

\item{label_size}{Font size (pts) for panel labels. Default: \code{8}.}

\item{bg}{Background colour for missing-image panels.
Default: \code{"#1a1a1a"}.}
}
\value{
Invisibly returns the assembled \code{gtable}; called for its
  side-effect of rendering the plot.
}
\description{
Retrieves JPG thumbnail paths stored by \code{\link{addThumbnailPaths}} and
renders them as a grid.  For \code{show = "both"} (default), BF and fluoro
images are placed \strong{side by side} within each well panel.  Multiple
wells are tiled according to \code{ncol} / \code{nrow}.
}
\examples{
\dontrun{
# Default: BF | fluoro side by side for each well
plotThumbnails(se, wells = c("B10.18T39383", "C05.18T39383"),
               img_class = "class1")

# 3 wells in a 2-column grid, show Condition in label
plotThumbnails(se,
               wells        = c("B10.18T39383", "B11.18T39383", "B12.18T39383"),
               img_class    = "class1",
               ncol         = 2,
               coldata_vars = "Condition")
}

}
