% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/currentClampTools.R
\name{runAPAnalysis}
\alias{runAPAnalysis}
\title{Run the AP analysis pipeline on a raw trace Parquet file}
\usage{
runAPAnalysis(
  raw_parquet_path,
  out_dir,
  cfg = NULL,
  plates = NULL,
  wells = NULL,
  verbose_every = 25L
)
}
\arguments{
\item{raw_parquet_path}{Path to a raw trace Parquet file produced by
\code{\link{csvToParquet}} (columns: \code{plate_id}, \code{well_id},
\code{sweep}, \code{t_s}, \code{recorded}, \code{stimulus}).}

\item{out_dir}{Directory where output Parquets will be written.  Created
if it does not exist.  Two files are written:
\code{<stem>_sweep.parquet} and \code{<stem>_ap.parquet}.}

\item{cfg}{A \code{\link{cc_config}} object.  \code{NULL} uses Python
defaults.}

\item{plates}{Optional character vector of plate IDs to process
(subset of plates in the Parquet).}

\item{wells}{Optional character vector of well IDs to process.}

\item{verbose_every}{Print progress every N wells. Default \code{25}.}
}
\value{
Named character vector with elements \code{sweep} and \code{ap}
  giving the paths of the written Parquet files (invisibly).
}
\description{
Step 2 of the current-clamp pipeline.  Calls the Python
\code{analyze_parquet_iterative()} function on \code{raw_parquet_path} and
saves the sweep-level and per-AP Parquet tables to \code{out_dir}.
}
\examples{
\dontrun{
paths <- runAPAnalysis(
  raw_parquet_path = "data-raw/CC/260304.parquet",
  out_dir          = "data-raw/CC/260304_results",
  cfg              = cc_config(dvdt_thr_mV_per_ms = 30)
)
se_cc <- prepareCCSE(paths["sweep"], ap_parquet_path = paths["ap"])
}

}
