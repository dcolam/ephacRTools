% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/currentClampTools.R
\name{csvToParquet}
\alias{csvToParquet}
\title{Convert a folder of per-well DataControl CSV files to a single Parquet file}
\usage{
csvToParquet(input_dir, output_parquet, plate_id = NULL, verbose_every = 20L)
}
\arguments{
\item{input_dir}{Path to a folder containing \code{*.csv} files (one per
well, one folder per plate).}

\item{output_parquet}{Destination \code{.parquet} path.  Parent directories
are created automatically.}

\item{plate_id}{Optional character string to override the plate ID parsed
from filenames (useful when the plate barcode is not embedded in the
filename).  Defaults to the folder name.}

\item{verbose_every}{Print progress every N files. \code{0} = silent.
Default \code{20}.}
}
\value{
Path to the written Parquet file (invisibly).
}
\description{
Step 1 of the current-clamp pipeline.  Each CSV in \code{input_dir} must
follow the DataControl naming convention
\code{<date>_<protocol>_<time>_<well>[_<plate>].csv}, e.g.
\code{241119_VI 5pA_16.10.11_A01__.csv}.  All CSVs are concatenated into
one long-format Parquet with columns \code{date}, \code{protocol},
\code{plate_id}, \code{well_id}, \code{sweep}, \code{sweep_time_s},
\code{t_s}, \code{recorded}, \code{stimulus}.
}
\examples{
\dontrun{
csvToParquet(
  input_dir      = "data-raw/CC/241119/22W14827/260304",
  output_parquet = "data-raw/CC/241119/22W14827/260304.parquet"
)
}

}
