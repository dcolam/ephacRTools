% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/currentClampTools.R
\name{cc_config}
\alias{cc_config}
\title{Build a current-clamp analysis configuration}
\usage{
cc_config(
  v_is_volts = TRUE,
  smooth_ms = 0.2,
  dvdt_thr_mV_per_ms = 25,
  refractory_ms = 2,
  peak_search_ms = 2,
  min_peak_voltage_mV = -20,
  min_height_mV = NULL,
  prominence_mV = NULL,
  baseline_win_s = 0.05,
  steady_win_s = 0.05,
  after_win_s = 0.05,
  after_delay_s = 0,
  stim_frac = 0.2,
  stim_min_dur_s = 0.02,
  stim_start_ignore_ms = 2,
  noise_win_s = 0.05,
  noise_mult_height = 6,
  noise_mult_prom = 5,
  min_height_floor_mV = 6,
  prominence_floor_mV = 4,
  phase_window_after_peak_ms = 3
)
}
\arguments{
\item{v_is_volts}{Logical; \code{TRUE} (default) if the recorded signal is
in Volts and should be multiplied by 1000 to obtain mV.}

\item{smooth_ms}{Gaussian smoothing window in ms before dV/dt computation.
Default \code{0.2}.}

\item{dvdt_thr_mV_per_ms}{dV/dt threshold for spike detection (mV/ms).
Default \code{25}.}

\item{refractory_ms}{Minimum inter-spike interval in ms. Default \code{2}.}

\item{peak_search_ms}{Window after dV/dt crossing to find voltage peak (ms).
Default \code{2}.}

\item{min_peak_voltage_mV}{Minimum voltage for a detected peak to count as a
spike (mV). Default \code{-20}.}

\item{min_height_mV}{Minimum spike height (mV). \code{NULL} (default) lets
the noise-adaptive floor take over.}

\item{prominence_mV}{Minimum spike prominence (mV). \code{NULL} (default).}

\item{baseline_win_s}{Baseline window duration before stim onset (s).
Default \code{0.05}.}

\item{steady_win_s}{Steady-state window duration before stim end (s).
Default \code{0.05}.}

\item{after_win_s}{Post-stimulus window duration (s). Default \code{0.05}.}

\item{after_delay_s}{Delay after stim end before measuring post-stim window
(s). Default \code{0}.}

\item{stim_frac}{Fraction of stimulus amplitude used to detect stim window.
Default \code{0.2}.}

\item{stim_min_dur_s}{Minimum detected stim duration (s). Default \code{0.02}.}

\item{stim_start_ignore_ms}{Ignore spikes within this window at stim onset
(ms). Default \code{2}.}

\item{noise_win_s}{Window used to estimate baseline noise (s).
Default \code{0.05}.}

\item{noise_mult_height}{Noise multiplier for adaptive height threshold.
Default \code{6}.}

\item{noise_mult_prom}{Noise multiplier for adaptive prominence threshold.
Default \code{5}.}

\item{min_height_floor_mV}{Hard minimum for noise-adaptive height (mV).
Default \code{6}.}

\item{prominence_floor_mV}{Hard minimum for noise-adaptive prominence (mV).
Default \code{4}.}

\item{phase_window_after_peak_ms}{Phase-plane analysis window after spike
peak (ms). Default \code{3}.}
}
\value{
A named list of class \code{"cc_config"}.
}
\description{
Constructs a named list of parameters that mirrors the Python
\code{EfelConfig} dataclass used by \code{ap_analysis_v2.py}.  Pass the
result to \code{\link{runAPAnalysis}}.
}
\examples{
cfg <- cc_config(dvdt_thr_mV_per_ms = 30, min_height_mV = 30)

}
