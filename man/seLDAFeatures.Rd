% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ldaTools.R
\name{seLDAFeatures}
\alias{seLDAFeatures}
\title{Build a scaled feature matrix from a SummarizedExperiment for LDA}
\usage{
seLDAFeatures(
  se,
  coldata_cols = NULL,
  assays = NULL,
  assay_sweeps = NULL,
  assay_agg = c("all", "mean", "sd"),
  scale = c("assay", "plate", "feature"),
  plate_col = "Plate_ID",
  wells = NULL,
  na_action = c("pass", "impute_zero"),
  drop_allna_cols = TRUE
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} or \code{SingleCellExperiment}.}

\item{coldata_cols}{Character vector of numeric \code{colData} columns.
\code{NULL} skips colData features.}

\item{assays}{Character vector of assay names to include.}

\item{assay_sweeps}{Sweep row indices (integer) or row names (character)
to subset.  \code{NULL} uses all sweeps.}

\item{assay_agg}{How to aggregate sweeps per well:
\code{"all"} (one column per sweep, default),
\code{"mean"}, or \code{"sd"}.}

\item{scale}{Character vector of scaling steps to apply in order.
Any subset of \code{c("assay", "plate", "feature")}.
Default is all three.}

\item{plate_col}{\code{colData} column containing plate identifiers.}

\item{wells}{Optional subset of well names (\code{colnames(se)}).
\code{NULL} uses all wells.}

\item{na_action}{How to handle \code{NA} values in the assembled feature
matrix before scaling.  \code{"pass"} leaves them as-is (wells with any
\code{NA} are dropped from LDA training by \code{\link{fitLDASE}} via
\code{complete.cases}); \code{"impute_zero"} replaces every \code{NA}
with \code{0} so no wells are dropped due to missing sweep data.}

\item{drop_allna_cols}{Logical.  If \code{TRUE} (default), columns where
every well is \code{NA} are silently removed before scaling.  Such columns
always have zero variance and would crash both scaling and LDA.}
}
\value{
A list with elements:
\describe{
  \item{\code{X}}{Numeric matrix \code{[wells × features]}.}
  \item{\code{spec}}{Feature name vector (column names of \code{X}).}
  \item{\code{scale_params}}{Scaling parameters for applying to new data.}
  \item{\code{assay_map}}{\code{data.frame} with \code{feature}, \code{assay},
    \code{sweep} columns — used by \code{\link{ldaLoadings}} to annotate results.}
}
}
\description{
Combines numeric \code{colData} columns and/or assay matrices into a single
\code{wells × features} matrix, applying up to three scaling steps.
}
