% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusteringTools.R
\name{clusterHeatmap}
\alias{clusterHeatmap}
\title{Heatmap of feature profiles per cluster}
\usage{
clusterHeatmap(
  se,
  feature_mat,
  cluster_col = "cluster",
  scale = TRUE,
  show_well_names = FALSE,
  ...
)
}
\arguments{
\item{se}{A \code{SingleCellExperiment}.}

\item{feature_mat}{Scaled feature matrix (wells × features) from
\code{\link{prepareClusterFeatures}}.}

\item{cluster_col}{Name of the \code{colData} column containing cluster
labels.  Default \code{"cluster"}.}

\item{scale}{Logical.  If \code{TRUE} (default), z-score each feature row
before plotting so that the colour scale reflects relative variation
within each feature rather than absolute magnitude.}

\item{show_well_names}{Logical.  Show column labels (well IDs).  Default
\code{FALSE} (too dense for full plates).}

\item{...}{Additional arguments forwarded to \code{ComplexHeatmap::Heatmap}.}
}
\value{
A \code{Heatmap} object.  Print or call
  \code{ComplexHeatmap::draw()} to render.
}
\description{
Renders a \code{ComplexHeatmap} with features as rows and wells as columns,
split by cluster assignment.  A row annotation distinguishes ephys from
morphology features when the feature matrix carries a \code{"modality"}
attribute (as returned by \code{\link{prepareClusterFeatures}}).
}
