% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/currentClampTools.R
\name{scanParquetForAPs}
\alias{scanParquetForAPs}
\title{Quick scan of a raw trace Parquet for AP-positive wells}
\usage{
scanParquetForAPs(
  parquet_path,
  v_is_volts = TRUE,
  threshold_mV = 0,
  sweeps = NULL
)
}
\arguments{
\item{parquet_path}{Path (or character vector of paths) to raw trace
Parquet file(s).}

\item{v_is_volts}{Logical; multiply \code{recorded} by 1000 to convert
V → mV.  Default \code{TRUE}.}

\item{threshold_mV}{Voltage threshold (mV) above which a well is considered
AP-likely.  Default \code{0}.}
}
\value{
A \code{data.frame} with columns \code{plate_id}, \code{well_id},
  \code{parquet_path}, \code{max_v_mV}, \code{n_sweeps}, \code{ap_likely};
  sorted AP-likely wells first (descending max_v_mV).
}
\description{
Computes the maximum recorded voltage and sweep count per well using Arrow
lazy evaluation (pure R — no Python required).  Wells where the maximum
voltage exceeds \code{threshold_mV} are flagged as \code{ap_likely = TRUE}.
}
