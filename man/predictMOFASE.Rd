% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusteringTools.R
\name{predictMOFASE}
\alias{predictMOFASE}
\title{Write MOFA2 factor scores to a SummarizedExperiment}
\usage{
predictMOFASE(se, mofa_fit, out_prefix = "mofa")
}
\arguments{
\item{se}{A \code{SummarizedExperiment} compatible with the SE used for
fitting (same well names).}

\item{mofa_fit}{Output of \code{\link{fitMOFASE}}.}

\item{out_prefix}{Prefix for new colData columns.  Default \code{"mofa"}.}
}
\value{
The \code{se} with factor score columns appended to
  \code{colData} and factor matrix stored in
  \code{reducedDims(se)[["MOFA"]]}.
}
\description{
Extracts factor scores from a fitted \code{mofa_fit_se} object and writes
them to \code{colData(se)} as \code{\{prefix\}_Factor1},
\code{\{prefix\}_Factor2}, \ldots — the same \code{out_prefix} convention
used by \code{\link{predictLDASE}} for LD scores.  Factor scores are also
stored in \code{reducedDims(se)[["MOFA"]]}.
}
