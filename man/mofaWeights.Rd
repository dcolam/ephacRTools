% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusteringTools.R
\name{mofaWeights}
\alias{mofaWeights}
\title{Feature weights from a fitted MOFA2 model}
\usage{
mofaWeights(mofa_fit, view = NULL, factor = NULL, n_top = NULL)
}
\arguments{
\item{mofa_fit}{Output of \code{\link{fitMOFASE}}.}

\item{view}{Character vector.  One or more view names to include.
\code{NULL} (default) returns all views.}

\item{factor}{Integer vector.  Which factors to include.
\code{NULL} (default) returns all factors.}

\item{n_top}{Return only the top \code{n_top} features per view by
importance.  \code{NULL} returns all.}
}
\value{
A \code{data.frame} with columns \code{feature}, \code{view},
  one column per selected factor (\code{Factor1}, \code{Factor2}, \ldots),
  \code{importance} (mean |weight| across factors), and \code{rank}.
}
\description{
Returns the per-feature weights for each view and factor, analogous to
\code{\link{ldaLoadings}} for LDA models.  Importance is defined as the
mean absolute weight across selected factors.
}
