% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusteringTools.R
\name{fitMOFASE}
\alias{fitMOFASE}
\title{Fit a MOFA2 multi-modal factor model on a SummarizedExperiment}
\usage{
fitMOFASE(
  se,
  mofa_features,
  n_factors = 5,
  scale_views = TRUE,
  seed = 42,
  use_basilisk = FALSE,
  condaenv = NULL,
  ...
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} (used for well alignment only;
the actual feature data is in \code{mofa_features}).}

\item{mofa_features}{Output of \code{\link{seMOFAFeatures}}.}

\item{n_factors}{Number of latent factors requested.  ARD priors will
shrink inactive factors toward zero; the actual number retained may be
lower.  Default \code{5}.}

\item{scale_views}{Logical.  Rescale views to equal total variance before
fitting (MOFA's cross-view balancing, applied on top of the per-view
scaling done in \code{\link{seMOFAFeatures}}).  Default \code{TRUE}.}

\item{seed}{Integer random seed.  Default \code{42}.}

\item{use_basilisk}{Logical.  Use \pkg{basilisk} for Python management.
Default \code{FALSE}.}

\item{condaenv}{Name of a conda environment with \code{mofapy2} installed.
When provided, \code{use_basilisk} is forced to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{MOFA2::run_mofa()}.}
}
\value{
An S3 object of class \code{mofa_fit_se} containing the fitted
  MOFA2 object, variance explained, factor scores, and all parameters
  needed by \code{\link{predictMOFASE}}, \code{\link{mofaWeights}}, and
  \code{\link{mofaVariance}}.
}
\description{
Takes the output of \code{\link{seMOFAFeatures}}, trains a MOFA2 model,
and returns a \code{mofa_fit_se} S3 object analogous to the
\code{lda_fit_se} returned by \code{\link{fitLDASE}}.
}
\seealso{
\code{\link{seMOFAFeatures}}, \code{\link{predictMOFASE}},
  \code{\link{mofaWeights}}, \code{\link{mofaVariance}}
}
