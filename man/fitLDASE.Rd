% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ldaTools.R
\name{fitLDASE}
\alias{fitLDASE}
\title{Fit a Linear Discriminant Analysis classifier on a SummarizedExperiment}
\usage{
fitLDASE(
  se,
  lda_features,
  label_col,
  method = c("global", "per_plate", "pca"),
  pca_var = 0.95,
  plate_col = "Plate_ID"
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment}.}

\item{lda_features}{Output of \code{\link{seLDAFeatures}}.}

\item{label_col}{\code{colData} column containing class labels.
Wells where this column is \code{NA} are excluded from training.}

\item{method}{Fitting strategy: \code{"global"} (one model for all plates),
\code{"per_plate"} (one model per plate, requires ≥ 2 classes per plate),
or \code{"pca"} (PCA dimensionality reduction then global LDA).}

\item{pca_var}{Fraction of variance retained by PCA when
\code{method = "pca"}.  Default \code{0.95}.}

\item{plate_col}{\code{colData} column for plate IDs (used by
\code{method = "per_plate"}).}
}
\value{
An S3 object of class \code{lda_fit_se} containing the model,
  scaling parameters, assay map, and composite loadings (always in original
  feature space, even for PCA-LDA).
}
\description{
Fit a Linear Discriminant Analysis classifier on a SummarizedExperiment
}
