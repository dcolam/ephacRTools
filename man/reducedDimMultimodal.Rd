% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusteringTools.R
\name{reducedDimMultimodal}
\alias{reducedDimMultimodal}
\title{Multi-modal dimensionality reduction with modality weighting}
\usage{
reducedDimMultimodal(
  se,
  ephys_cols = NULL,
  morpho_cols = NULL,
  assay_names = NULL,
  ephys_weight = 1,
  morpho_weight = 1,
  scale_method = c("zscore", "minmax", "none"),
  na_action = c("impute_median", "impute_zero", "omit"),
  ephys_na_action = NULL,
  morpho_na_action = NULL,
  n_pcs = 50,
  method = c("pca", "tsne", "umap"),
  k_clusters = 3,
  cluster_method = c("kmeans", "hierarchical", "louvain", "gmm"),
  seed = 42
)
}
\arguments{
\item{se}{A \code{SingleCellExperiment}.}

\item{ephys_cols}{colData columns for ephys scalar features.}

\item{morpho_cols}{colData columns for morphology features.}

\item{assay_names}{Assay names to include as full IV-curve feature blocks.}

\item{ephys_weight}{Relative variance contribution of the ephys block.
Default \code{1}.}

\item{morpho_weight}{Relative variance contribution of the morphology block.
Default \code{1}.}

\item{scale_method}{Feature scaling within each block.  Default
\code{"zscore"}.}

\item{na_action}{NA handling.  Default \code{"impute_median"}.}

\item{n_pcs}{Maximum number of PCs to retain.  Default \code{50}.}

\item{method}{Character vector of reductions to compute.  Any combination
of \code{"pca"}, \code{"tsne"}, \code{"umap"}.  Default all three.}

\item{k_clusters}{Number of clusters for the clustering step.  Default
\code{3}.}

\item{cluster_method}{Clustering algorithm passed to \code{\link{clusterSE}}.
Default \code{"kmeans"}.}

\item{seed}{Random seed.  Default \code{42}.}
}
\value{
\code{se} with \code{reducedDims} populated (\code{PCA}, \code{TSNE},
  \code{UMAP} as requested) and \code{colData(se)$cluster} set.
}
\description{
Builds a modality-weighted feature matrix via
\code{\link{prepareClusterFeatures}}, runs PCA, and optionally t-SNE and
UMAP, storing results in \code{reducedDims(se)}.  Cluster labels are
computed on the PCA coordinates and stored in
\code{colData(se)$cluster}.
}
\details{
This function supersedes the legacy \code{reducedDim.Cellwise()}.
}
\examples{
\dontrun{
se <- reducedDimMultimodal(
  se,
  ephys_cols    = c("Imax.minima", "Vhalf.minima", "Capacitance_mean"),
  morpho_cols   = c("GFP_Mean_z", "GFP_normArea"),
  ephys_weight  = 1,
  morpho_weight = 1,
  k_clusters    = 4
)
}

}
