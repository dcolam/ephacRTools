% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/currentClampTools.R
\name{plotCCTraces}
\alias{plotCCTraces}
\title{Plot raw current-clamp traces}
\usage{
plotCCTraces(
  parquet_path,
  well_id = NULL,
  plate_id = NULL,
  sweeps = NULL,
  v_is_volts = TRUE,
  stim_scale = 1e+12,
  stim_unit = "pA",
  alpha = 0.65,
  linewidth = 0.45,
  palette = "plasma",
  color = NULL,
  scalebar = FALSE,
  inset_x = 1,
  inset_y = 0,
  inset_size = 0.38,
  inset_alpha = 0,
  scalebar_lwd = 0.6,
  scalebar_size = 2.8,
  scale_main = c(NA, NA),
  scale_inset = NA,
  ncol = 4
)
}
\arguments{
\item{parquet_path}{Path to a raw trace Parquet.}

\item{well_id}{Character vector of wells to plot (e.g. \code{c("A01","B02")}).
\code{NULL} plots all wells in the parquet.}

\item{plate_id}{Optional plate filter. \code{NULL} (default) includes all
plates in the parquet.}

\item{sweeps}{Integer vector of sweep numbers to include. \code{NULL} = all.}

\item{v_is_volts}{Multiply \code{recorded} by 1000 (V → mV). Default \code{TRUE}.}

\item{stim_scale}{Factor applied to \code{stimulus} for display.
Default \code{1e12} (A → pA).}

\item{stim_unit}{Label for the stimulus scalebar. Default \code{"pA"}.}

\item{alpha}{Line transparency. Default \code{0.65}.}

\item{linewidth}{Line width for voltage traces. Default \code{0.45}.}

\item{palette}{Viridis palette name. Default \code{"plasma"}.}

\item{color}{Fixed line colour (e.g. \code{"black"}). Overrides
\code{palette} when set. Default \code{NULL}.}

\item{scalebar}{Replace main-panel axes with voltage + time scalebars.
Default \code{FALSE}.}

\item{inset_x}{Horizontal anchor of the stimulus inset as a 0–1 fraction
of the x range. \code{0} = left, \code{1} = right. Default \code{1}.}

\item{inset_y}{Vertical anchor of the stimulus inset as a 0–1 fraction
of the y range. \code{0} = bottom, \code{1} = top. Default \code{0}.}

\item{inset_size}{Inset width as a fraction of the x range. Default \code{0.27}.}

\item{ncol}{Number of columns when faceting multiple wells. Default \code{4}.}
}
\value{
A \code{ggplot} object (single well) or a faceted \code{ggplot}
  (multiple wells, no inset).
}
\description{
Reads a raw trace Parquet produced by \code{\link{csvToParquet}} and plots
sweeps overlaid, coloured by sweep number (viridis gradient).  A small
stimulus inset shows the injected current waveform.  Pass a vector of well
IDs to \code{well_id} to get a faceted multi-well overview.
}
\examples{
\dontrun{
# Single well
plotCCTraces("18T37419.parquet", "G10")

# Multiple wells — faceted grid
plotCCTraces("18T37419.parquet", c("G10", "G11", "H01", "H02"))

# Minimal style
plotCCTraces("18T37419.parquet", "G10", scalebar = TRUE, color = "black")
}

}
